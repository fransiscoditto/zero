<%= form_for(@blogcomment) do |f| %>
  <% if @blogcomment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blogcomment.errors.count, "error") %> prohibited this blogcomment from being saved:</h2>

      <ul>
      <% @blogcomment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %><br />
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :blog_id %><br />
    <%= f.number_field :blog_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
